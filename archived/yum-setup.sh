#!/bin/sh
#make sure curl and cli are installed
yum install php-curl php-cli
#make sure the perl modules are installed
yum install libimage-exif-perl libspreadsheet-writeexcel-perl libtext-csv-xs-perlhistor libgd-graph-perl

